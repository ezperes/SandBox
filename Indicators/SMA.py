def SMA(data: "iterable", periods_nr: int, reference: int = 0) -> float:
    """ Returns the Simple Moving Average from a set of data

        SMA = SUM(data)/count(data)

            :data:iterator: The base data to be calculated
            :periods_nr:integer: The number of periods to be calculated
            :reference:integer: Given the ordered data set, considers 0 the last one, and n-1 the first one
                default: 0
            :return:float: The calculated SMA
    """


    pass
